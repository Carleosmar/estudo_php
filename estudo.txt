Objetivo de cada tag:

header: cabeçalho
main: principal
footer: Ropadé

----------------------------

Grid: definir linhas e colunas e colocar os elementos dentro dessa linha e coluna




9EV3RB

sentença de codigo php termina com ;

ctrl + alt + j: Para selecionar a linguagem para codar no
vscode.
-------------------------------------------
PRECEDENCIA
()
**
/ OU *
+ OU -
------------------------------------
.: Utilizado para concatena.

CONSTANTE VALOR QUE NÃO PODE SER ALTERADO

&&: and
||: or


indice: [] : Representar a posição ocupada no array.


Função serve reutilizar codigos.


escopo: Se refere que a Variavel que está dentro da função

Argumentos:

Numa função é melhor usar um return do que um echo. Pois será mais facil altera os dados da função.

&$xxxxxx: Endereço da variavel

callable: garante se uma função pode ser chamada

use: Ferramenta utilizada para retorna uma outra função.

Map: serve para tranformar um array em outro array,
elemento em outro elemento.

filter é o que faz a transformação.

map: Usar uma função que ja ésta pronta.

filter:Trás uma função. 
------------------------------------------------
POO

Classe:Toda classe tem um bloco de codigo associado. Que é exatamente o corpo da classe.

Corpo: tem atributos e comportamentos que por sua vez tem metodos(funções)

Classe tem atributos e metodos.

Todo objeto do mundo real tem atributos e comportamentos assim como um classe.

classe define um tipo(Curso, aluno, tutor).

classe representa uma abstração <- simplificação do mundo real
conceitos poderem ser similar abstração não.

Classe representa a estrutura de dados.

Objeto: quando se criar um dado apartir de uma classe falamos que criamos um objeto da classe.
objeto pode ser chamado de instaciar.

Classe pode criar N objetos.

Construtor é um metodo especial que é focado em criar objetos a partir 
de classe. Construtor gera um outro objeto

Classe é a estrutura de dados e o objeto é o dado.

Membros são atributos e comportamentos em outra palavars usado
para referenciar tudo que pertence a uma classe.

Destrutor é chamado quando um objeto ta para ser destruido

Inicial das palavras classe deve ser com letra maiuscula

public: modificador de visibilidade

new: Usado para criar um objeto a partir de uma classe

this: apontar para o objeto ou instaciar atual.

unset: usado para chamar o destrutor

paradgma prodedural é baseado em procedimentos.

Prosedural(função)

O.O(dado)

Objeto é o que tem uma função.

Encapsulamento: É referente um metodo existente que não sabemos como funcionar.

Capsula->OBJETO->atributo, metodo

o encapsulamento é feito pelos modificadores de acesso.

Private o membro é visivel apenas dentro da classe.

protected: O atributo é visivel apenas dentro da classe e é transmitido por herança

public:O atributo é visivel apenas dentro da classe e é transmitido por herança
e ainda é visivel para qualquer outro codigo acessar aquele membro



































